apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: bookwatch-front
spec:
  serviceName: "front"
  replicas: 1
  selector:
    matchLabels:
      app: front
  template:
    metadata:
      labels:
        app: front
    spec:
      containers:
      - name: front
        image: ghcr.io/aitorru/bookwatch-front-b13ea43de9715c93232d503e427659c93ec5f3f9:trunk
        ports:
        - containerPort: 3000
        env:
          - name: BACK_HOST
            value: back
