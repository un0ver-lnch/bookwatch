apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: bookwatch-front
spec:
  serviceName: "front"
  replicas: 1
  selector:
    matchLabels:
      app: front
  template:
    metadata:
      labels:
        app: front
    spec:
      containers:
      - name: front
        image: ghcr.io/aitorru/bookwatch-front-0a7385e148fd994fb37589fe6ae78baf4e31035c:trunk
        ports:
        - containerPort: 3000
        env:
          - name: BACK_HOST
            value: back
